🧹 Remove dead commented code in SettingsForm.cs

🎯 **What:** Removed commented-out `LocalizationHelper.Export();` code in `SettingsForm.cs`.
💡 **Why:** Dead commented-out code clutters the codebase and reduces readability and maintainability. Removing it simplifies the file.
✅ **Verification:** Verified by checking that tests still compile and run, with no change in functionality.
✨ **Result:** A cleaner codebase.
