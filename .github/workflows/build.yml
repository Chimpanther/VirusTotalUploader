name: Build .NET Framework Desktop

# RETIRED 2026-08-16: Windows build migrated to Travis CI (WINDOWS_TRAVIS).
# This workflow is kept for manual archival runs only.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build
    if: ${{ false }}
    runs-on: windows-latest
    steps:
      - run: echo "retired"
